# Github_Azure_foundry

This repository now includes a Node.js "Hello World" project.

## Node.js sample

- `package.json` - Node.js project configuration
- `index.js` - entry point

### Run

```bash
npm install
npm start
```

## Existing Terraform config

(unchanged existing files `main.tf`, `providers.tf`, `variables.tf`, `outputs.tf`)
